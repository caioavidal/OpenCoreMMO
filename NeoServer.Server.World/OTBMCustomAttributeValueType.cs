namespace NeoServer.Server.World {

	/// <summary>
	/// This enum is used to simplify the switch described in TFS's item.h's line 293.
	/// </summary>
	public enum OTBMCustomAttributeValueType : byte {
		String = 1,
		Int64 = 2,
		Double = 3,
		Bool = 4,
	}
}
