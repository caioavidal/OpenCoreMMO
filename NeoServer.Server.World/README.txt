The classes in this directory / namespace are used to load .otbm maps.
We are using the term "world" instead of "map" for two reasons:
1) We are actually loading a game world.
2) To make the difference between these classes and the old ones (Map namespace) more explicit.